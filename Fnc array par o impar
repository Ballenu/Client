var numbers = [2, 5, 6];

function isPair(numbers){
    var pairity = false;
    for(var i = 1; i <= numbers.length; i++){
        if(numbers[i] % 2 == 0) pairity = true;
        return pairity;
    }
}

console.log(isPair(pairity));
