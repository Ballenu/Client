var numbers = [2, 5, 6];

function isPair(numbers){
    var pairity = [];
    for(var i = 0; i < numbers.length; i++){
        pairity.push(numbers[i] % 2 == 0);
    }
    return pairity;
}

console.log(isPair(numbers));
