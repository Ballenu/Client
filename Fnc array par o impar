var numbers = [2, 5, 6];

function isPair(numbers){
    var pairity = [];
    for(var i = 0; i < numbers.length; i++){
        if(numbers[i] % 2 == 0) pairity.push(true);
        else pairity.push(false);
    }
    return pairity;
}

console.log(isPair(numbers));
